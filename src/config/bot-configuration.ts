import {BotConfigurationInterface} from "./bot-configuration.interface";

export const botConfiguration: BotConfigurationInterface = {
    client: {
        owner: "208733560151080961",
        commandPrefix: "$"
    }
}
